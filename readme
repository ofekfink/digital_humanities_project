divide to 2 :

* train on true xmls without errors, see what happens on docx.
* train on faulted xmls, see what happens on true / faulted xmls. see what happens on docxs.

# we are basically using transfer learning
